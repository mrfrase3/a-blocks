var time = arguments[0];
var delta = arguments[1];
if(!this.vars['_interval_{{id}}']) this.vars['_interval_{{id}}'] = time;
if(time - this.vars['_interval_{{id}}'] < ({{sec}})*1000) return;
this.vars['_interval_{{id}}'] = time;
